{% extends "layout.njk" %}

{% block content %}
<main class="container">
  <h1>{{ title }}</h1>
  <p>{{ message }}</p>
  {% for bird in birds %}
    <p>fågel {{ bird.id }}</p>
    <p>{{ bird.species }}</p>
    <p style="font-weight: bold"><a href="/birds/{{bird.id}}">{{ bird.name }}</a></p>
    <p style ="margin-bottom: 10%">{{ bird.wingspan }} cm vingspann</p>
  {% endfor %}
</main>
{% endblock %}